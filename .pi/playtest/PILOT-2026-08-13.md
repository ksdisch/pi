# Constellation co-op pilot, run 4 — planet-1, 2026-08-13

Two runs, back to back, against constellation `604a422` — the first co-op play
since the platform fix (`8318f82`, PR #40) and the telemetry fixes (PR #21)
both landed. Seats: laptop `google/gemini-3.5-flash-lite`, phone
`google/gemini-3.1-flash-lite`.

Run A `20260813-145350`: ~3.5 min, 5 deaths, no clear.
Run B `20260813-151453`: ~5.5 min, 10 deaths, no clear.
Both ended when the laptop seat was killed by a token-rate 429, not by either
seat choosing to stop.

**The question this pilot exists to answer: can a pair clear planet-1 now, and
where does coordination still tax them?**

**No clear — but the wall moved to exactly where the game fix was supposed to
put it, and this pilot measures the new one instead of guessing at it.** Pilot
3's blocker was a stopwatch: a 5s platform and a 3s freeze that had to overlap
and never did. That blocker is gone. What replaced it is an *ordering* problem
with a clean signature: **when the laptop armed before asking, it worked — twice
in run B, both times reaching the pit lip. When the phone cast before the laptop
armed, it failed every time**, in three distinct ways, including two 90-second
deadlocks where both seats were blocked simultaneously.

Runs 1-3 are `PILOT-2026-08-11.md`, `PILOT-2026-08-11-run2.md`, and
`PILOT-2026-08-12.md`.

## Rails first

`verify-rails.sh` cleared planet-1 on attempt 1 against the fixed level:
freeze → corridor to x=620 → platform summoned in 4860ms → `jumpAtX` leap onto
it (y=366) → settle → second leap → `won:true`. The scripted solution still
works, so both runs below test the pair, not a broken level.

## The platform fix, measured in live co-op

This is the cleanest result in the pilot and it needs no reconstruction.

In run A the phone's `summon-platform` solve returned at **19:54:31.199**. The
laptop then read `/state` four times — 19:55:04.948, 19:55:15.375,
19:56:49.017, 19:57:25.267 — and every one reported `platformCount: 1`. The
astronaut never landed on it (every run-A death was at ground level or in the
pit), so the countdown never started.

**The platform stood unlanded-on for at least 2m54s.** Under the pre-#40 rule —
5.0s from the drop — it would have vanished at 19:54:36, before the laptop's
first look at it. Run B reproduced this: one platform cast at 20:16:35, still
`platformCount: 1` at 20:20:27, ~3m52s later, across nine intervening moves.

The game also tells the phone player about the new rule, in its own words. The
cast-feedback text the driver captured reads:

> Platform — bridge waits, then holds for a few seconds once they step on.

That is the fix surfacing in the UI, not just in the physics.

## Deaths — built only from driver-logged events

Pilot 3's first draft misattributed deaths by reconstructing them from partial
data. With `diedAt` shipped, no reconstruction is needed: every row below is a
field copied from a `/move` reply.

### Run A — 5 deaths

| Time | Events | `armedForMs` | `diedAt` | Read |
|---|---|---|---|---|
| 19:54:13.689 | `respawned` | — | `{388, 476}` | sentry |
| 19:54:21.486 | `arm-fired`, `respawned` | 925 | `{772, 592}` | pit |
| 19:55:13.652 | `arm-fired`, `respawned` | 2951 | `{780, 608}` | pit |
| 19:56:55.185 | `arm-fired`, `respawned` | 983 | `{772, 592}` | pit |
| 19:57:32.066 | `respawned` | — | `{424, 476}` | sentry |

### Run B — 10 deaths

| Time | Events | `armedForMs` | `diedAt` | Read |
|---|---|---|---|---|
| 20:16:14.416 | `respawned` | — | `{408, 476}` | sentry |
| 20:16:26.894 | `arm-fired`, `respawned` | 1172 | `{776, 600}` | pit |
| 20:16:41.252 | `arm-fired`, `respawned` | 61 | `{516, 476}` | sentry (armed on platform, no freeze) |
| 20:18:50.251 | `respawned` | — | `{340, 476}` | sentry |
| 20:19:00.769 | `arm-fired`, `respawned` | 1358 | `{776, 600}` | pit |
| 20:19:09.214 | `arm-fired`, `respawned` | 61 | `{268, 476}` | sentry (stale freeze) |
| 20:19:17.580 | `respawned` | — | `{356, 476}` | sentry |
| 20:20:06.985 | `respawned` | — | `{772, 592}` | pit (from x=644, no jump) |
| 20:20:15.085 | `arm-fired`, `respawned` | 61 | `{276, 476}` | sentry (stale freeze) |
| 20:20:24.385 | `jumped`, `respawned` | — | `{772, 592}` | pit (jumped at 680) |

The `y` does the whole job — 476 is standing height, 592/600/608 is already
falling — with no arithmetic in between. Compare pilot 3, where all nine deaths
reported `state.x` of 80-168 and the report had to reconstruct the real sites
from elapsed times and a ceiling-collision model.

**Zero `respawned-while-armed` across both runs**, including two 90-second
holds. The laptop armed from near spawn each time, exactly as its prompt
teaches, and standing still there never cost it anything.

## The coordination tax has a signature

Arm ordering decides the outcome, and both runs say so independently.

**Arm first, then cast — works.** Run B, twice:

| Armed at | Cast returned | `armedForMs` | Result |
|---|---|---|---|
| 20:19:19.545 | 20:20:02.736 (43s later) | 43032 | `reached-x`, parked x=640, `enemyFrozen: true` |
| 20:20:19.489 | 20:20:20.578 (1.1s later) | 927 | `reached-x`, parked x=636 |

Both crossed the entire patrol band and stopped safely at the pit lip. The 43s
hold is the affordance working exactly as designed: the laptop committed, the
phone took its time, and the move fired on arrival.

**Cast first, then arm — fails, three ways.**

*Way 1 — the 90-second deadlock, in both runs.* Run A: the phone's freeze solve
returned 19:55:10.584, closing at ~19:55:13.6. The laptop armed at
**19:55:17.688**, 4.1s after the window shut, and asked for a cast it had
already spent. The phone replied "Already cast! You should be able to run now.
Did you make it?" at 19:55:19.235, then blocked in `intercom_wait` from
19:55:22.408 to 19:56:50.597. **For 88 seconds the phone waited for a report
while the laptop waited for a cast the phone believed it had made.** The arm hit
its 90s ceiling at 19:56:47.724. Run B repeated it: cast returned 20:17:08.034,
laptop armed 20:17:12.870 (1.8s late), `arm-timeout` at 20:18:42.911,
`armedForMs` 90027. That single stall is 27% of run B's playable life.

*Way 2 — the false green light.* Three run-B arms report `armedForMs: 61` —
they fired essentially instantly, because the trigger reads the *level* and the
freeze was already on. But it was nearly over. Two of the three are exact:

- Armed 20:19:08.140 on a freeze cast at 20:19:05.703 (closing ~20:19:08.7).
  Fired in 61ms; died at x=268, which the astronaut reaches at 0.73s — **20:19:08.93,
  0.2s after the thaw.**
- Armed 20:20:13.947 on a freeze cast at 20:20:11.937 (closing ~20:20:14.9).
  Fired in 61ms; died at x=276, reached at 0.78s — **20:20:14.9, at the thaw.**

An instant `arm-fired` reads to the seat as "the window is open." It isn't; it
is closing. Level-triggering is right for the platform, which waits
indefinitely, and misleading for a 3s freeze.

*Way 3 — the un-armed round trip.* Run A's laptop tried it deliberately once:
the phone's solve returned 19:57:28.722 and announced at 19:57:29.497; the move
started 19:57:30.577 — **1.9s of a 3.0s window spent on the round trip.** The
astronaut entered the band still frozen and thawed mid-band, dying at x=424. Run
B's two un-armed attempts died the same way, at x=340 and x=356.

## The safe waiting spot has no run-up

This is the new blocker, and run B found it without ever seeing the level.

The laptop invented a two-stage crossing: `untilX: 630` to park at the pit lip
under freeze, then a separate move to cross. Stage one works — it parked at
x=640 and x=636, both times with the sentry still frozen. Stage two never did:

- From x=644, a plain `{dir:"right", ms:4000}` ran straight off the edge:
  `diedAt {772, 592}`.
- From x=640, `{dir:"right", ms:3000, jumpAtX:680}` produced `jumped` and then
  `diedAt {772, 592}` anyway.

The fall edge is at x≈656, so a jump commanded at x=680 is commanded past the
lip. And the rails script clears this pit by jumping at **x=620 with a running
start** — from x=640 there is only 16px of ground left and no speed to carry.

**The spot that is safe to wait in is the spot that cannot make the jump.** That
is the coordination tax in one sentence: the pair must either cross the band and
the pit in a single freeze window, or park somewhere that costs them the leap.

## Findings

1. **The platform fix works, and it moved the wall** *(Fact — the arc's
   question, answered)*. Platforms stood ≥2m54s (run A) and ~3m52s (run B)
   unlanded-on, observed directly. Pilot 3's recommendation — "a platform that
   persists until crossed makes the pit a coordination problem instead of a
   stopwatch" — is implemented and behaves as specified in live co-op, not just
   in solo headless runs. No seat ever lost a bridge to expiry.

2. **The pit is now a run-up problem, not a stopwatch problem** *(Fact — new)*.
   With a permanent bridge, run B still put four deaths in the pit. The pair
   solved the sentry (two clean parks at x≈640 under freeze) and then could not
   convert, because parking kills the momentum the jump needs and the only
   safe parking spot is past the useful launch point. *(Recommendation, and the
   highest-value change on this list)* Either widen the landable approach — let
   the platform be reachable from a standing start at the lip — or give the
   astronaut a reason to be at x≈600 rather than x≈640 when the freeze lands.
   The fix should make "get to the edge safely" and "have enough run-up" the
   same position; today they are 40px and one failed jump apart.

3. **`diedAt` retired the misattribution, completely** *(Fact — the telemetry
   arc, closed)*. Fifteen deaths across two runs, every one reported at a real
   site. No seat quoted a respawn coordinate. Pilot 3's finding #3 — a cast
   requested against a hazard 570px from the actual death — **did not
   reproduce**: run A's laptop named "x=388 on the ground (patrolling enemy)"
   and asked for Freeze Stars, then named "x=650-770" and asked for Summon
   Platform, both correct first try; run B's laptop correctly diagnosed its own
   x=516 death as "crossing with platform because enemy wasn't frozen." The
   phone's couch glance carried it too — at 19:57:01 it told its partner "Looks
   like you died at x=772 again," reading `worldLastDeath`, not `world.x`.

4. **`arm-timeout` is a cast/arm ordering race, not "no cast came"** *(Fact —
   new; pilot 3 had zero timeouts to observe)*. All three timeouts across both
   runs began after the freeze window had already closed (4.1s, 1.8s, and 0.9s
   late). Two of them blocked both seats for ~88s. *(Recommendation)* Fix this
   in the protocol, not the driver: the prompts should make "arm, *then* ask"
   the stated default and tell the phone to hold a cast until its partner says
   it is armed. A driver-side fix — arming against a cast that is about to
   happen — would be the harness inventing coordination the players didn't do,
   and should not be built.

5. **An instant `arm-fired` is a false green light on a short window** *(Fact —
   new)*. Three run-B arms fired in 61ms on an already-running freeze and two of
   them died within 0.2s of the thaw, at x=268 and x=276. The trigger is
   level-based by design, and that is correct for the platform; for a 3s freeze
   it hands the player a go signal with a fraction of a second behind it.
   *(Recommendation — harness, for the next pilot)* Have `/move` report the
   trigger's remaining life alongside `arm-fired` (e.g. `freezeMsLeft`), so a
   seat can tell "just cast" from "about to lapse" and report it. This is
   observability, not assistance — it tells the player what a human watching the
   screen would already see.

6. **The support seat's ask has sharpened from "I'm blind" to "tell me where"**
   *(Fact — fourth run running, but different in kind)*. Runs 1-3 all produced
   "I can't tell what killed them". With `worldLastDeath` supplying that, run
   A's phone asked for something more specific: "The phone doesn't tell me
   *where* the platform appears, only that it is summoned. If the platform
   appears in a location that doesn't solve the pit jump, I have no way to
   diagnose it." That is a game-UI finding, not a harness one — the couch glance
   does not carry platform position either, and a pair in separate rooms has
   neither.

7. **A token-rate 429 killed the laptop seat in both runs** *(Fact —
   harness/pi, not the game)*. Both died on
   `GenerateContentInputTokensPerModelPerMinute-FreeTier` (250k input
   tokens/min); run A after ~3.5 min with `Please retry in 24.9s`, run B after
   ~5.5 min with `Please retry in 34.1s`. This is the known fail-fast-by-design
   path (BACKLOG Active item 5); nothing was patched mid-pilot. The seat turns
   roughly 6 times a minute with a context that grows every turn, so the cap is
   a function of how *briskly* it plays, not just how long. Worth noting the
   perverse interaction with finding 4: the 90-second deadlocks are the only
   thing that kept either run alive as long as it was, because standing blocked
   burns no tokens.

8. **Puzzle solve times remain stable to within milliseconds across four
   pilots** *(Fact — driver transcripts)*. Run A: Freeze Stars 669, 669, 671,
   671, 673 ms (n=5); Summon Platform 4845 ms. Pilot 3 measured 672-676 and
   4835-4869. The ~7× earn-time spread is unchanged, and it is what makes the
   freeze the scarce resource now that the platform is not.

## Honesty caveats

- **Armed moves are driver-reflex.** The seat chose the maneuver, the trigger,
  and when to arm; the driver held it and fired ~200ms after the cast landed.
  Every sentry traverse in this pilot was made under an armed move, including
  both of run B's successful parks at x≈640 — the headline of "arm first works"
  is armed-move progress, and should be read with that discount.
- **The driver solved every puzzle.** The phone chose which power and when, and
  critiques from the transcript.
- **The couch glance is a harness affordance, not a game feature.** The phone
  correctly citing "you died at x=772" came from `worldLastDeath`; the phone
  screen shows none of it. Finding 6 is graded on the phone screen, as the
  prompt requires.
- **Both runs are truncated** and neither laptop report was written — both seats
  were killed by the 429 before that step, so their running notes are what they
  left. Both phone seats outlived their partners and kept casting into a game
  nobody was driving; run A's phone wrote its report after detecting the
  silence, and every phone-seat quotation in this report comes from run A's
  report or from the two seats' notes files.
- **Death counts are not a quality metric.** Runs 1-4 had different durations
  and different interventions; no trend should be read across them.
- Headless — neither seat saw pixels. Unlike pilot 3, this report's central
  claims are read from logged fields rather than reconstructed. Three numbers
  are arithmetic and flagged as such: the freeze-window closures (solve-return
  timestamp plus the game's own stated 3s), the platform spans (cast-return to
  last observation), and the two thaw-timing coincidences in "Way 2" (240 px/s
  from the logged `before.x` to the logged `diedAt.x`). Those two are the most
  load-bearing inferences here and both land within 0.2s;
  `reports/video/20260813-151453/laptop.webm` is the way to falsify them.
- `diedAt` lags the true death by up to one 60ms poll (~14px), and on a fall it
  is where the fall registered, not the edge that was left — the pit rows'
  x=772-780 bound the x≈656 edge from the right, they do not locate it.

## Appendix — artifacts

Run A `20260813-145350`, run B `20260813-151453`. Per-seat reports and notes
under `reports/`, video under `reports/video/<RUNID>/`, intercom under
`.pi/intercom/playtest-<RUNID>/`. All git-ignored. Per-turn driver replies —
the source for every number above — are in the pi session transcripts under
`~/.pi/agent/sessions/--Users-kyledisch-Projects-pi--/`.

One harness note from setup, not a game finding: an orphaned vite from an
earlier session held :5180 while its relay half was dead, so `run-pilot.sh`
skipped starting the dev server (it probes :5180 only) and then timed out
waiting on :3081. Killing the orphan fixed it. The readiness probe treats a
two-service dev server as one, and only the game half is checked before the
decision to start it.
