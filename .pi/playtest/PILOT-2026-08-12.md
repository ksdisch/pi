# Constellation co-op pilot, run 3 — planet-1, 2026-08-12

Run `20260812-004303`. Seats: laptop `google/gemini-3.5-flash-lite`, phone
`google/gemini-3.1-flash-lite`. ~8 minutes of play, **11 deaths, no clear**,
against a goal at x=920. The run ended when the laptop seat was killed by a
429, not by either seat choosing to stop.

This is the first pilot with **armed moves** (`arm:{on:"freeze"|"platform"}`,
merged as `8cd563be9`), which exist to answer one question: with the
cue-to-cast round trip removed, does a coordinated pair clear planet-1?

**No — but not for the reason the first two pilots predicted, and not for the
reason this report originally claimed.** Armed moves worked, and they worked
better than any seat realized: **all three moves that got past the sentry died
at the same pit lip, x≈655–665** — roughly 100px short of the platform drop that
would have bridged it, and ~250px past the furthest position either seat could
see (x=400).

The wall is not the freeze window and not coordination latency. It is that
crossing the pit needs Summon Platform (~4.9s to earn, 5s of life, fixed drop at
x=770) alive at the same moment as a Freeze Stars (3s) that gets you past the
sentry — two windows from two puzzles that this pair never once overlapped.

Runs 1 and 2 are `PILOT-2026-08-11.md` and `PILOT-2026-08-11-run2.md`.

## The first attempt was void — read this before trusting any pilot artifact

Run `20260812-003752` launched first and produced a **phantom clear**: driver
state read `won:true`, `completed:{"planet-1":true}`, planet-2 unlocked. It was
not our pair's clear. It was not a game bug either.

`~/Projects/party-line` contains a **copy of this harness**, and a session there
started its own run (`20260812-003954`) while ours was mid-play. Both default to
driver ports 4801/4802, and `run-pilot.sh` step 4 calls `stop_drivers` before
spawning its own — so each run shut the other's drivers down and took the ports.

The evidence is unambiguous, and none of it is inference:

| Observation | What it proves |
|---|---|
| Our seat's `/screenshot` replied with a path under `party-line/playtest/reports/shots/` | A party-line driver process served our seat's calls on 4801 |
| Our teardown printed `video saved: .../party-line/playtest/reports/video/20260812-003954/` | Our orchestrator shut down *their* drivers |
| Our own videos flushed at 00:38 — mid-run, ~30s long | Our drivers were killed by their `stop_drivers` while our pair was still playing |
| Our seat saw `lastCastPower:"summon-platform"` before our phone had cast anything | We were reading their game (room EESPAC), not ours (room ADRYPB) |
| Their `logs/rails-run2.txt` ends `rails NOT verified` with JSON decode errors | Our teardown broke their run in the same way |

Our pair issued exactly **one** move in that run and **zero** armed moves; the
x=92 → x=892 jump to the win had no move command behind it at all. The run
answers nothing and is not written up further.

*(Fact — harness defect, not a game finding.)* Two checkouts of this harness on
one machine silently corrupt each other and each produces a plausible-looking
result. *(Recommendation)* Derive the default driver ports from the harness path
(or mint them per run) instead of hardcoding 4801/4802, and have `stop_drivers`
refuse to shut down a driver whose `/health` reports a different `HARNESS_DIR`.
The re-run used `LAPTOP_DRIVER_PORT=4811 PHONE_DRIVER_PORT=4812` and was clean —
its videos and screenshots landed in this repo, as they should.

## Did armed moves work?

Mechanically, yes — completely. Three armed moves, three fires, no failures of
the mechanism:

| Time | `arm.on` | `armedForMs` | Events | Died at (reconstructed) |
|---|---|---|---|---|
| 05:43:35 | freeze | 1050 | `arm-fired`, `respawned` | x≈659 — pit lip, past the sentry |
| 05:49:25 | freeze | 864 | `arm-fired`, `respawned` | x≈665 — in the pit, past the sentry |
| 05:49:33 | platform | 4626 | `arm-fired`, `respawned` | x≈448 — sentry band, no freeze up |

**Zero `arm-timeout`. Zero `respawned-while-armed`.** Every arm caught its cast,
and standing armed never once killed the astronaut — the exposure the design doc
worried about (0.9s, 4.6s of standing still) cost nothing on this level.

All three still ended in a death — but *where* they died is the finding, and no
seat could see it. Two of the three cleared the sentry entirely.

## What the wall actually is

Every pilot so far has had its seats report deaths at coordinates near spawn:
this run's laptop logged "died at x=84 / 88 / 92 / 96" and told its partner so.
**Those are not death sites — they are respawn sites.** After a death, `/move`
returns the state *after* the respawn, so the x a seat reads is where the game
put it back, not where it was killed. The laptop prompt's own advice — "compare
x before/after to learn where the danger was" — reads the wrong number. This
report's first draft made the same mistake one level up, and the correction is
what produced its actual result.

Reconstructing from planet-1's geometry and measured travel times gives the real
picture. The astronaut runs at 240 px/s (`Astronaut.ts:15`) from spawn at x=80,
and the sentry patrols `corridor.x=420 ± 140`, i.e. **x=280–560**. Three details
matter and all three are easy to miss:

- **The fall edge is x≈656, not the config's `pit.startX: 660`.** The physics
  never reads that value for support — `Planet.ts:145` only uses it to skip tile
  creation. Ground tiles are 64px wide (`Boot.ts:25`) on centres 32…928, so the
  last solid span before the pit is 576–640; a 32px astronaut keeps footing until
  its centre passes 640 + 16.
- **A pit death registers only after the fall** past `fallRespawnY: 600`
  (`Planet.ts:369-372`), which from the ground at y=476 under gravity 900 takes
  sqrt(2·124/900) ≈ **0.525s** of the elapsed time.
- **There is a ceiling over the corridor.** `Planet.ts:150` puts a 400×20 block
  (`Boot.ts:26`) at `corridor.x`, spanning **x 220–620, y 350–370**, with a
  collider at `:264`. Running under it is free. A jump that is *still rising when
  its right edge reaches the block's left face* is pinned there instead, and the
  cost depends on where the jump started, not on the block's width: the body top
  is inside the band only for t=0.230–0.792s after take-off (jump −460,
  gravity 900, 32×48 sprite), and the right edge reaches x=220 at
  t=(204−x_jump)/240, so **pin = 0.792 − (204−x_jump)/240**, and only for
  x_jump between ~14 and ~149. Outside that range the cost is **zero** — a jump
  started under the block hits the underside, is separated in Y with
  `velocity.x` untouched, and slides on at the full 240 px/s. Only the one
  `jumped` row below is in range, at ~0.40s.

Elapsed times carry two more biases, both from the driver's 60ms poll
(`laptop.mjs:223-254`) and both worth stating rather than folding in. A death is
detected up to a poll late, so every reconstructed x runs **0–15px high** — the
run's own non-death rows show it, with `reached-x` moves coming out at 229 and
236 px/s apparent against a true 240. And `jumpAtX` fires on the first sample
past its threshold, so the one `jumped` row's take-off is x_jump ∈ [100, 114.4]
and its pin is **0.359–0.419s**, not a point value.

All nine in-move deaths, not the four that fit a story:

| Time | Events | From x | Elapsed | Reconstructed death x | Where |
|---|---|---|---|---|---|
| 05:43:23 | `respawned` | 80 | 1239 ms | ≈ 377 | sentry band |
| 05:43:29 | `respawned` | 88 | 1121 ms | ≈ 357 | sentry band |
| 05:43:35 | `arm-fired`, `respawned` | 88 | 2906 ms | ≈ **659** | **pit lip** |
| 05:44:13 | `respawned` | 80 | 1237 ms | ≈ 377 | sentry band |
| 05:49:25 | `arm-fired`, `respawned` | 96 | 2894 ms | ≈ **665** | **pit** |
| 05:49:33 | `arm-fired`, `respawned` | 92 | 1483 ms | ≈ 448 | sentry band |
| 05:49:41 | `jumped`, `respawned` | 92 | 3270 ms | ≈ **655** (−0.40s ceiling) | **pit lip** |
| 05:50:32 | `respawned` | 80 | 1605 ms | ≈ 465 | sentry band |
| 05:51:08 | `respawned` | 168 | 551 ms | ≈ 300 | sentry band |

**Three moves reached the fall edge at x≈656, and all three stop there** — 659,
665, and 655, from three different start positions (88, 96, 92), under three
different `ms` budgets (3000, 4000, 4000), one of them via a jump that costs
~0.40s to a ceiling the other two never touched. Carrying the poll biases through
puts the true departures at roughly 644–659, 650–665 and 635–665; all three
brackets contain 656. Three independent moves landing on one edge by three
different routes is a pit fall, not arithmetic luck.

The sentry did not kill any of the three, and for each one there is a cast that
says so. `Planet.ts:282-285` early-returns the overlap while `enemy.isFrozen`.
The two armed rows fired on `enemyFrozen` going true within ~260ms (60ms poll +
the pinned 200ms reaction), leaving ≥2.74s of `FREEZE_DURATION_MS = 3000` to run
when the move started. Row 7 carried no `arm` at all — its cover comes from the
phone transcript: a `freeze-stars` solved at **05:49:37.709**, so the window ran
to ~05:49:40.71, while row 7's move started 05:49:38.06 and crossed x=280→560 at
05:49:39.25–40.42. Wholly inside.

That same cast is the run's sharpest illustration of the overlap problem: it
landed **0.2s after the platform expired** (summoned 05:49:32.547, 5s life). The
pair had a freeze and a bridge within a quarter-second of each other and still
never held both at once.

So armed freeze did exactly what it was built to do: it carried the astronaut
through the entire patrol band, twice, and set it down at the edge of a chasm
with no bridge. The third armed move (`on:"platform"`, `armedForMs` 4626) died
at x≈448 — in the band — because it fired on a platform with no freeze running.

*(Recommendation, and the highest-value change on this list)* Have the driver
capture x/y at the moment `respawnCount` increments and return it as `diedAt`,
leaving `state` as the post-respawn truth. It is a small driver change, it costs
the game nothing, and it retires a misattribution that has now distorted the
seats' reasoning in all three pilots and this report's own first draft.

## Findings

1. **Armed moves solved the sentry; the pit is what planet-1 is actually made
   of** *(Fact — the arc's question, answered)*. Runs 1 and 2 concluded the ~3s
   freeze could not survive a ~12s chat turn and named the freeze window as the
   problem. Remove the chat turn and the freeze is not tight at all: crossing
   the whole band, spawn to far edge, is (560−80)/240 = **2.00s**, and with the
   driver's pinned 200ms reaction and 60ms poll that is 2.26s against a 3.00s
   freeze — 0.74s of slack, confirmed empirically by two armed moves that
   finished 100px *past* the band. The blocker is one step later: the pit at
   x=660–880 is crossed by a platform that costs **~4.9s to earn and lives
   5.0s**, dropping at a fixed x=770. A pair must hold a 3s freeze and a 5s
   platform open simultaneously, from two puzzles with a 7× solve-time spread.
   This pair never once overlapped them. *(Recommendation)* The lever is the
   platform's earn-vs-life ratio, not the freeze timer and not the patrol band:
   a platform that persists until crossed (or whose 5s starts when the astronaut
   reaches it) makes the pit a coordination problem instead of a stopwatch.

   *This supersedes the first draft of this report, which claimed no death was a
   pit fall and that the pair never reached x=660. Both were wrong — the death
   table had been built from four of the nine in-move deaths, dropping exactly
   the three armed ones. Caught in adversarial review; the corrected table is
   above.*

2. **Standing still killed the astronaut twice with no move in flight**
   *(Fact — now measured, not inferred)*. Two deaths landed with no move in
   flight: `respawnCount` went 7→8 and then 8→9 in the gaps *between* driver
   calls, each time resetting the astronaut from x=264 and x=400 back to spawn
   while the seat was composing intercom messages. (The following 9→10 death did
   happen during a move.) Runs 1 and 2 both asserted "waiting for your partner
   is lethal"; this is the first run where the deaths are visible in the gaps
   between commands rather than attributed after the fact. The co-op loop
   requires waiting and the level punishes it.

3. **A cast was spent on a hazard that was 570px away** *(Fact)*. The laptop's
   first death drew "Can you cast Illuminate?" — the dark zone sits at x=920 and
   the astronaut died around x=377. Run 1's finding #5 called this out; it
   reproduced exactly, because the cause (finding above: seats read respawn x)
   was never fixed. The dark zone *reads* as the threat when you cannot see.

4. **The support seat is blind and says so, again** *(Fact — third run running)*.
   The phone: "The lack of clarity on what exactly was causing the death (enemy,
   pit, or something else) made my support role feel blind," and its one change
   is "more explicit feedback for the Phone player about the state of the world."
   Run 1's finding #4 and run 2's finding #3 are the same ask. Three independent
   phone seats have now volunteered it.

5. **Puzzle solve times are stable across runs and seats** *(Fact — driver
   transcripts)*. Twelve casts, all solved: Illuminate 187ms, Freeze Stars
   672–676ms (n=5), Summon Platform 4835–4869ms (n=5). The ~7× spread run 2
   measured holds to within 35ms across a different session. Summon Platform
   costs ~4.9s to earn and lasts ~5s.

6. **A token-rate 429 killed the laptop seat mid-run** *(Fact — harness/pi,
   not the game)*. The seat died on
   `GenerateContentInputTokensPerModelPerMinute-FreeTier` (250k input
   tokens/min), whose body stated `Please retry in 32.308s` and carried a
   `RetryInfo` of 32s. pi's classifier treats token-rate quotas as fail-fast by
   design — only per-minute *request*-rate quotas were made retryable by the run-2
   fix. A playtest seat resends a growing context every turn, so it walks into
   the input-token cap the longer it plays: the failure mode gets *more* likely
   the better the run is going. *(Recommendation)* Worth revisiting whether a
   token-rate 429 that ships an explicit `RetryInfo` should be retryable on the
   same floored-backoff path as the request-rate case. Flagging, not fixing —
   it is a change to shipped pi retry behavior, not to the harness.

## Honesty caveats

- **Armed moves are driver-reflex.** The seat chose the maneuver, the trigger,
  and when to arm; the driver held it and fired ~200ms after the cast landed.
  That pause is pinned in the driver and not caller-tunable. Progress made under
  an armed move is pre-committed anticipation, not a live read — and this run's
  headline result *is* armed-move progress (two clean sentry traverses), so read
  it with that discount applied. A human holding a key in anticipation is the
  claim; a frame-perfect script is not.
- **The driver solved every puzzle**, not the phone player, as in every prior
  run. The phone chose which power and when. Note it reported Illuminate as
  "~4s" in its own write-up where the driver transcript says 187ms — a seat
  estimate, not a measurement.
- **The run is truncated.** ~8 minutes, ended by the 429 rather than by either
  seat judging it done. The pair may have improved with more turns; 11 deaths
  is where it stood when the seat died, not a plateau either seat declared.
- **Deaths are 11 by `respawnCount`**, read from driver state. Run 1 was 25 and
  run 2 was 11, but the three runs had different durations and different
  interventions — the counts are not comparable as a measure of play quality,
  and no trend should be read into them.
- Headless; neither seat saw pixels. **The death-site reconstruction is
  arithmetic, not observation** — start x plus 240 px/s × `elapsedMs`, minus the
  0.525s fall on rows that reach the edge and the ~0.40s ceiling pin on the one
  `jumped` row — and it now carries this report's main claim, so it deserves the
  scrutiny. Every figure is an over-estimate by 0–15px (the 60ms poll), and the
  one `jumped` row carries a second ±30ms from the same poll on `jumpAtX`; both
  are stated where they matter rather than folded in. What makes it more than a
  plausible fit: three rows converge on the x≈656 fall edge by three different
  routes, a named cast rules out the sentry for each of the three, and the
  `jumped` row's unexplained 395ms falls inside the 359–419ms the ceiling
  independently predicts. It has not been checked against pixels;
  `reports/video/20260812-004303/laptop.webm` recorded this run and is the way to
  falsify it.

## Appendix — artifacts

Per-seat reports `reports/20260812-004303-{laptop,phone}.md` (the laptop seat's
was never written — it was killed before that step; its running notes at
`reports/20260812-004303-laptop-notes.md` are what it left), video at
`reports/video/20260812-004303/`, intercom at
`.pi/intercom/playtest-20260812-004303/`. All git-ignored. Per-turn driver
replies — the source for every number above — are in the pi session transcripts
under `~/.pi/agent/sessions/--Users-kyledisch-Projects-pi--/`, which is the only
place a completed run's raw driver state survives.
