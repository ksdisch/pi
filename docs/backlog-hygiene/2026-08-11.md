# Backlog Hygiene — 2026-08-11

Scope: full (groom + sequence + decide). Appetite: one arc. Lean: build new.
Excluded: Constellation playtest work (owned by another session).

## Corpus

No backlog file existed. Corpus assembled from: handoff extension DESIGN.md
"out of scope for v1 (v2 hooks)" list, project memory (autonomous-sessions
north star), open fork PRs, recent git log.

## Findings

| Item (source) | Verdict | Evidence | Disposition |
|---|---|---|---|
| Context-fullness watcher (DESIGN.md v2 hooks) | fresh | no `getContextUsage` call in any `.pi/extensions` file | keep — arc candidate |
| Autonomous successor spawning (DESIGN.md + memory) | too-big; first slice ready | manual confirm-gated spawn shipped (`.pi/extensions/handoff/index.ts:226`); `kickoff` contract live (`notes.ts:38`, `digest.ts:246`) | decomposed — watcher slice first |
| Slack/Telegram ping at decision points (DESIGN.md) | fresh, sequenced-after | nothing built; value requires unattended sessions | keep, after spawning |
| Repo-scoped event ledger (DESIGN.md) | too-big / underspecified | no design doc; seed = dated `.pi/handoffs/` dir | keep, design-first |
| Walk-up note detection / cross-cwd routing (DESIGN.md) | fresh, no observed pain | all fork work at repo root | keep, low priority |
| PR #12 (docs CLAUDE.md pointer) | landing chore | open, docs-only | land; not backlog |
| PR #10 (retry clamp follow-ups) | excluded | playtest scope, other session owns it | no action |
| `tui-plan.md` at root | not fork backlog | upstream file (last touched `583f153d5`) | ignore |

## Arcs considered

- **Arc A (picked): "Sessions that notice they're full"** — context-fullness
  watcher + auto-proposed handoff, reusing the shipped spawn path. Blast
  radius: `.pi/extensions/handoff/` only. Well-specified, no design phase
  needed.
- Arc B: full autonomous spawn + messaging loop-in — phase-sized; Arc A is a
  prerequisite. Deferred.
- Arc C: event-ledger design + walk-up routing — no current consumer; weakest
  fit for the build-new lean. Deferred.

## Coverage note

Post-groom, the active pipeline is 100% north-star build items: no
solidify/bug items, no UX items. Matches the stated lean; if a reserve of
quick wins or hardening items is wanted later, that is a `/replenish`
(QuickWin/Harden lanes) job.

## Outcome

Arc A picked. BACKLOG.md created recording the groomed ledger (no invented
items). Handoff routed to `/autonomous-milestone` on Opus 5, effort high.
