# Backlog Hygiene — 2026-08-15

Scope: full (groom + sequence + decide). Appetite: an arc of a few items.
Off-limits: nothing. Ran the session after pilot 8's clear, alongside the
red-CI fix (PR #32), the pilot 8 docs pass (PR #33), the pilot 9 plan
(PR #34), and the Slack loop-in design pass (PR #35).

## Corpus

`BACKLOG.md` (8 active items), the pilot 8 review mailbox's open follow-ups
(`~/.claude/reviews/pi/2026-08-14-feat-playtest-pilot-8.md`), the PR #32
review's follow-ups (`~/.claude/reviews/pi/2026-08-15-fix-red-ci.md`), the
upstream draft (`docs/upstream/newsession-on-extension-context.md`), git log,
fork PR state. Issues are disabled on the fork; no `docs/ideas/`.

## Findings

| Item | Verdict | Evidence | Disposition |
|---|---|---|---|
| Slack/Telegram loop-in | fresh, design-first | zero code under `.pi/extensions/`; prerequisite shipped in #18/#19 | design pass ran this session (PR #35); credentials stay Kyle-action; resequenced to 3 |
| Repo-scoped event ledger | too-big | no design doc in `docs/build-plans/` | keep; first slice stays "write the design doc" |
| Walk-up note detection | fresh, low | still no observed pain | keep |
| Playtest review follow-ups | fresh, partially softened | F5's raw-TypeError half gone (`requireBooted()` throws descriptively) but the mid-boot window stands (`booted` flips at `laptop.mjs:301`, guard checks `page`); `verify-rails.sh` still doesn't tee | keep; grown with pilot 8 leftovers (F5/F7/F8/F10) and PR #32 nits (F1/F3/F4) as a dated sub-list |
| Trigger remaining life | fresh | run B: zero arm-timeouts, but instant-fire-on-standing-platform unchanged | keep |
| Upstream `newSession()` post | fresh, Kyle-action | branch `feat/expose-newsession-to-events` + draft exist, unposted | keep; PR #32's F2 noted as a second upstream candidate |
| `recentDeaths` on `/state` | fresh, small | sampler queues already; pilot 8: 3 of 12 sampler-only deaths reached a seat | sequenced to 1 — pilot 9 arc |
| Stop handoff-digest injection | fresh, small | no gating in `run-pilot.sh`; injection documented in all four seats, pilots 6–7 | sequenced to 2 — pilot 9 arc |

No retirements: nothing stale, duplicate, or shipped-already.

## Arcs considered

- **Arc A (picked): Pilot 9 — the composition experiment.** The trigger-bound
  composition rule (the experiment), plus `recentDeaths`, the sampler
  blind-window fix (pilot 8 review F6), and the digest opt-out — F11 riding
  the prompt touch. Highest information: answers the standing open question
  (is run A's never-jump prompt-fixable?) while every riding change gets the
  run it needs to be measured. Blast radius `.pi/playtest/` only. Plan:
  `docs/build-plans/2026-08-15-pilot-9-composition.md` (PR #34).
- Arc B: Slack loop-in design → build — top-sequenced north star, but
  credentials gate the build mid-arc and it is phase-shaped; the design half
  ran this session anyway (PR #35), so the build is ready whenever the
  credentials exist.
- Arc C: solidification sweep over the grown follow-up list — cheap, zero new
  information; fine as a filler arc later.

## Coverage note

Post-groom mix: experiment (pilot 9 arc), build-new (Slack build, ledger,
walk-up), solidification (follow-ups, trigger-life), two Kyle-chores
(upstream post, Telegram credentials). No empty axis; no `/replenish` needed.

## Outcome

Arc A picked (Kyle, at the decision gate). BACKLOG.md resequenced: pilot 9
arc items to 1–2, Slack loop-in (design done) to 3, follow-ups item grown,
upstream item carries the F2 candidate. Starter prompt below; run config per
the plan doc.

## Appendix — starter prompt (pilot 9 arc)

Run config: Opus 5, effort high.
`cd ~/Projects/pi && claude --model claude-opus-5 --effort high`

```
You are working in Kyle's fork ksdisch/pi (upstream earendil-works/pi). Read
AGENTS.md and CLAUDE.md first — the fork git workflow applies: feature branch
→ PR to ksdisch/pi main (pass --repo ksdisch/pi explicitly) → adversarial-
review loop → merge. The playtest harness is zero-diff on the constellation
repo; everything lands under .pi/playtest/ only.

Execute pilot 9 per docs/build-plans/2026-08-15-pilot-9-composition.md — read
it in full first, then .pi/playtest/PILOT-2026-08-14-run8.md for the
baseline numbers and report conventions.

Build (one PR, adversarial review before merge):
1. The trigger-bound composition rule in .pi/playtest/prompts/laptop.md, plus
   the F11 fix (soften line ~57's absolute "only a lastStoodAt past the gap
   proves you got across" to the no-evidence/only-positive-proof form).
   Prompts stay level-generic — never name coordinates.
2. recentDeaths on /state (short newest-first list beside lastDeath; the
   sampler already queues).
3. Sampler keeps lastSeen across the death tick (clear y1/y2/lastStood as
   today — the spawn point would poison the rest test).
4. Handoff-digest opt-out: run-pilot.sh launches seats with the handoff
   extension disabled or env-gated.
Editing .pi/playtest/*.mjs with string-replace tools fights tab indentation —
use a small node script for edits and run node --check after. Update
DESIGN.md and both prompts where the contract changes.

Run (after the PR merges): two runs via run-pilot.sh with
PILOT_TIMEOUT_S=2100. Note: "run-pilot.sh drivers" leaves drivers up but NOT
booted — /boot, phone /join, /await-phone before /planet. The freeze power id
is freeze-stars. Tally every number from the driver ledger
(logs/<RUNID>-laptop-driver.log, one "death rc=… via=… kept=…" line per
placed death; via means whose copy was kept, NOT who saw it) plus the seat
transcripts (~/.pi/agent/sessions/…, UTC names, laptop identified by "You
are **Laptop**").

Report: PILOT-<date>-run9.md per the plan's measurement list — primary is
composition per seat (baseline 1-of-2 across pilots 7–8); state the
several-changes-at-once caveat with the per-channel attribution the plan
pre-commits. Then move the two shipped BACKLOG items to Shipped with the PR
number and queue what the run surfaces.

Delegate to a subagent only for large, genuinely independent, parallelizable
tracks; the adversarial-review pre-merge loop is exempt and still runs.
```
